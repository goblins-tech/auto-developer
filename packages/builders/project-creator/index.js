/*
initiate a workspace, create some files such as package.json, .gitignore, tsconfig.json, ...
 */

const fs = require("fs");

//todo: use Schematics to return Rule, don't physically modify the project's files
export default function(options, creator) {
  //returns a Schematics Rule to initiate the workspace
}
