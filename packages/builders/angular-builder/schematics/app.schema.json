{
  "$schema": "http://json-schema.org/schema",
  "id": "projectBuilderInit",
  "title": "init a workspace",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "x-prompt": "app name",
      "description": "app's name"
    },
    "path": {
      "type": "string",
      "defult": "/src",
      "x-prompt": "project path (default: '/')",
      "description": "app's root path"
    }
  },
  "required": ["name"]
}
