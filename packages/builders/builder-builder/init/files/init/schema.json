{
  "$schema": "http://json-schema.org/schema",
  "id": "<%=classify(options.name)%>BuilderInit", <%# todo: camelCase(name)%>
  "title": "setup the builder.",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "defult": "/",
      "x-prompt": "project path (default: '/')",
      "description": "project's root path"
    }
  },
  "required": []
}
